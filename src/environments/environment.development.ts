// Reemplaza a environment.ts durante `ng serve` / build "development" via fileReplacements
// (ver angular.json). Apunta al backend corriendo en local con `npm run dev`.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api/v1',
};
